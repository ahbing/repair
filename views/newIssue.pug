extends layout

block content
  p 这是一个多美丽又遗憾的世界啊！！！
  form(action="/issue" method="POST")
    input(type="text",name="title")
    input(type="hidden", name="user", value=user.githubId)
    br
    textarea(name="content")
    br
    input(type="submit", value="publish")
