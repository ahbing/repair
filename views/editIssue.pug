extends layout

block content
  p 这是一个多美丽又遗憾的世界啊！！！
  form(action="/issue/update" method="POST")
    input(type="text",name="title", value=issue.title)
    input(type="hidden", name="id", value=issue._id)
    br
    textarea(name="content") #{issue.content}
    br
    input(type="submit", value="update")
